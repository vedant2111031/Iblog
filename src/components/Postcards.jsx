import React from 'react'
import appwriteService from "../appwrite/config"
import {Link} from 'react-roucter-dom'


function Postcards({$id,title,featuredImage}) {
  return (
    <Link to={`/post/${$id}`}>
        
    </Link>
  )
}

export default Postcards
